{% extends 'AdminBundle:User:skeleton.html.twig' %}
{% block radrest_content %}
    <div class="panel-heading">
        <h3 class="panel-title">Edit</h3>
    </div>
    <div class="panel-body">
        {% if form is defined %}
            {{ form(form, {action: path('admin_user_put', {id: id}), style: 'horizontal'}) }}
        {% endif %}
    </div>
{% endblock %}
